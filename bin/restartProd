#!/bin/bash

docker-compose -f ./docker/docker-compose.production.yml -p news-feed pull webapp
docker-compose -f ./docker/docker-compose.production.yml -p news-feed down
docker-compose -f ./docker/docker-compose.production.yml -p news-feed up -d
